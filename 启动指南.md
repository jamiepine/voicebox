# Voicebox 中文启动指南

<p align="center">
  <img src=".github/assets/icon-dark.webp" alt="Voicebox" width="120" height="120" />
</p>

<h1 align="center">Voicebox</h1>

<p align="center">
  <strong>开源 AI 语音工作室</strong><br/>
  本地优先的语音克隆、语音生成、全局听写、AI 代理语音输出
</p>

---

## 📋 目录

- [环境要求](#环境要求)
- [快速开始（双端启动）](#快速开始双端启动)
- [后端启动（Conda 方式）](#后端启动conda-方式)
- [后端启动（uv 方式，推荐）](#后端启动uv-方式推荐)
- [前端启动（Bun 方式）](#前端启动bun-方式)
- [桌面应用开发](#桌面应用开发)
- [Web 版本开发](#web-版本开发)
- [常见问题](#常见问题)

---

## 🔧 环境要求

在开始之前，请确保已安装以下工具：

| 工具 | 版本要求 | 说明 |
|------|----------|------|
| **Conda** (Miniconda/Anaconda) 或 **uv** | 最新版 | Python 环境管理（推荐 uv，更快更现代） |
| **Python** | 3.12+ | 后端运行环境 |
| **Bun** | >= 1.0.0 | JavaScript/TypeScript 运行时与包管理器 |
| **Git** | 最新版 | 版本控制 |
| **Rust** | 最新稳定版 | 仅桌面应用（Tauri）需要 |

### 方式一：安装 uv（推荐）

uv 是一个极速的 Python 包管理器，比 pip/conda 快 10-100 倍：

```bash
# Linux/macOS 一键安装
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows PowerShell
# powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# 重启终端或执行
source ~/.bashrc  # 或 source ~/.zshrc
```

### 方式二：安装 Conda

如果你更熟悉 Conda，可以使用 Miniconda（轻量版）：

```bash
# Linux/macOS 下载安装 Miniconda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 或访问官网下载：https://docs.conda.io/en/latest/miniconda.html
```

### 安装 Bun

```bash
# Linux/macOS 一键安装
curl -fsSL https://bun.sh/install | bash

# 重启终端或执行
source ~/.bashrc  # 或 source ~/.zshrc
```

验证安装：

```bash
uv --version       # 如果用 uv
# 或 conda --version
python --version
bun --version
```

---

## 🚀 快速开始（双端启动）

提供两种后端环境管理方式：**uv（推荐，更快）** 和 **Conda**，搭配 **Bun** 管理前端依赖，双端并行启动。

### 第一步：克隆项目

```bash
git clone https://github.com/jamiepine/voicebox.git
cd voicebox
```

### 第二步A：配置后端（uv 方式，推荐）

```bash
# 1. 在项目根目录创建 Python 3.12 虚拟环境（自动创建 .venv）
uv venv --python 3.12

# 2. 激活虚拟环境
# Linux/macOS:
source .venv/bin/activate
# Windows:
# .venv\Scripts\activate

# 3. 安装 PyTorch（根据你的硬件选择，使用 uv pip）

# --- NVIDIA GPU (CUDA 12.8) ---
uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

# --- AMD GPU (ROCm 6.3) ---
# uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.3

# --- Apple Silicon (Mac M系列) ---
# uv pip install torch torchaudio

# --- CPU 版本（无GPU加速） ---
# uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# 4. 安装后端依赖（uv 会自动解决依赖冲突）
uv pip install -r backend/requirements.txt

# 5. 安装特殊依赖
uv pip install --no-deps chatterbox-tts
uv pip install --no-deps hume-tada

# 6. 安装 Qwen3-TTS
uv pip install git+https://github.com/QwenLM/Qwen3-TTS.git

# 7. （可选）Apple Silicon 安装 MLX 加速
if [[ "$(uname -m)" == "arm64" && "$(uname)" == "Darwin" ]]; then
    uv pip install -r backend/requirements-mlx.txt
    uv pip install --no-deps mlx-lm==0.31.1
    uv pip install --no-deps mlx-audio==0.4.1
fi

# 8. 安装开发工具
uv pip install pyinstaller ruff pytest pytest-asyncio
```

### 第二步B：配置后端（Conda 方式）

```bash
# 1. 创建并激活 conda 环境
conda create -n voicebox python=3.12 -y
conda activate voicebox

# 2. 升级 pip
pip install --upgrade pip

# 3. 安装 PyTorch（根据你的硬件选择）

# --- NVIDIA GPU (CUDA 12.8) ---
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

# --- AMD GPU (ROCm 6.3) ---
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.3

# --- Apple Silicon (Mac M系列) ---
# pip install torch torchaudio

# --- CPU 版本（无GPU加速） ---
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# 4. 安装后端依赖
pip install -r backend/requirements.txt

# 5. 安装特殊依赖（无依赖冲突安装）
pip install --no-deps chatterbox-tts
pip install --no-deps hume-tada

# 6. 安装 Qwen3-TTS
pip install git+https://github.com/QwenLM/Qwen3-TTS.git

# 7. （可选）Apple Silicon 安装 MLX 加速
if [[ "$(uname -m)" == "arm64" && "$(uname)" == "Darwin" ]]; then
    pip install -r backend/requirements-mlx.txt
    pip install --no-deps mlx-lm==0.31.1
    pip install --no-deps mlx-audio==0.4.1
fi

# 8. 安装开发工具
pip install pyinstaller ruff pytest pytest-asyncio
```

### 第三步：配置前端（Bun 依赖）

```bash
# 在项目根目录执行，安装所有工作区依赖
bun install
```

### 第四步：双端并行启动

打开**两个终端**窗口，分别启动后端和前端：

#### 终端 1：启动后端（选择你配置的环境）

**uv 方式：**
```bash
# Linux/macOS
source .venv/bin/activate
# Windows: .venv\Scripts\activate
uvicorn backend.main:app --reload --port 17493
```

**或 Conda 方式：**
```bash
conda activate voicebox
uvicorn backend.main:app --reload --port 17493
```

后端启动成功后，访问 http://127.0.0.1:17493/docs 可以看到 API 文档。

#### 终端 2：启动前端（Web 版本）

```bash
# 启动 Web 版本前端
cd web
bun run dev
```

前端启动后，访问 http://localhost:5173 即可使用。

---

## 🐍 后端启动（Conda 方式）

### 常用后端启动命令

```bash
# 确保已激活 conda 环境
conda activate voicebox

# 方式1：标准开发模式（自动重载）
uvicorn backend.main:app --reload --port 17493

# 方式2：指定主机地址（允许局域网访问）
uvicorn backend.main:app --reload --host 0.0.0.0 --port 17493

# 方式3：直接运行 main.py
python -m backend.main --host 127.0.0.1 --port 17493

# 方式4：自定义数据目录
python -m backend.main --data-dir ./data
```

### Conda 环境管理

```bash
# 创建环境
conda create -n voicebox python=3.12 -y

# 激活环境
conda activate voicebox

# 退出环境
conda deactivate

# 查看所有环境
conda env list

# 删除环境（谨慎使用）
conda env remove -n voicebox

# 导出环境配置
conda env export > environment.yml

# 从配置文件创建环境
conda env create -f environment.yml
```

### 后端代码检查与测试

```bash
conda activate voicebox

# 代码检查
ruff check backend/

# 代码格式化
ruff format backend/

# 自动修复 lint 问题
ruff check backend/ --fix

# 运行测试
python -m pytest backend/tests -v
```

---

## ⚡ 后端启动（uv 方式，推荐）

uv 是 Astral 推出的极速 Python 包管理器，用 Rust 编写，比 pip/conda 快 10-100 倍，依赖解析更可靠。

### 常用后端启动命令

```bash
# 方式1：激活虚拟环境后启动（推荐）
# Linux/macOS:
source .venv/bin/activate
# Windows:
# .venv\Scripts\activate

# 然后启动
uvicorn backend.main:app --reload --port 17493

# 方式2：不激活环境，直接用 uv run（更方便）
uv run uvicorn backend.main:app --reload --port 17493

# 方式3：指定主机地址（允许局域网访问）
uv run uvicorn backend.main:app --reload --host 0.0.0.0 --port 17493

# 方式4：直接运行 main.py
uv run python -m backend.main --host 127.0.0.1 --port 17493

# 方式5：自定义数据目录
uv run python -m backend.main --data-dir ./data
```

> 💡 **提示**：使用 `uv run` 无需手动激活虚拟环境，uv 会自动检测项目根目录的 `.venv` 并使用它。

### uv 环境管理

```bash
# 创建虚拟环境（指定 Python 版本）
uv venv --python 3.12

# 创建时指定 Python 版本路径
# uv venv --python /usr/bin/python3.12

# 激活虚拟环境
# Linux/macOS:
source .venv/bin/activate
# Windows:
# .venv\Scripts\activate

# 退出虚拟环境
deactivate

# 删除虚拟环境
rm -rf .venv

# 查看当前 Python 路径（确认是否在虚拟环境中）
which python  # Linux/macOS
# where python  # Windows

# 安装包（在激活的环境中）
uv pip install <package-name>

# 安装开发依赖
uv pip install --dev <package-name>

# 从 requirements.txt 安装
uv pip install -r backend/requirements.txt

# 查看已安装的包
uv pip list

# 升级包
uv pip install --upgrade <package-name>

# 卸载包
uv pip uninstall <package-name>

# 同步依赖（根据 pyproject.toml 精确同步环境）
uv sync
```

### uv 常用技巧

```bash
# 一次性运行脚本（无需激活环境）
uv run python script.py

# 运行 pytest 测试
uv run pytest backend/tests -v

# 运行 ruff 检查
uv run ruff check backend/
uv run ruff format backend/
uv run ruff check backend/ --fix

# 添加依赖并写入 pyproject.toml
uv add <package-name>
uv add --dev <package-name>  # 开发依赖

# 锁定依赖版本
uv lock

# 锁定依赖并安装
uv sync
```

### 为什么推荐 uv？

| 特性 | uv | Conda | pip + venv |
|------|----|-------|-----------|
| 🚀 速度 | 极快（Rust 编写，比 pip 快 10-100 倍） | 慢 | 中等 |
| 📦 依赖解析 | 快速且可靠 | 容易出现冲突 | 较慢，容易卡住 |
| 🐍 Python 版本管理 | ✅ 自动下载管理 Python | ✅ 可以安装不同 Python 版本 | ❌ 使用系统 Python |
| 🪶 轻量级 | ✅ 单二进制文件，无全局环境 | ❌ 较重，base 环境复杂 | ✅ 轻量 |
| 🔒 锁文件 | ✅ uv.lock 保证可复现构建 | ✅ environment.yml | ❌ requirements.txt 不可靠 |
| 📂 项目隔离 | ✅ 项目内 .venv，自动识别 | ❌ 全局环境目录 | ✅ 项目内 venv |
| 🌐 跨平台 | ✅ Windows/macOS/Linux 一致 | ✅ 跨平台 | ✅ 跨平台 |

---

## 🟨 前端启动（Bun 方式）

### Web 版本开发

```bash
# 在 web 目录下启动开发服务器
cd web
bun run dev

# 构建生产版本
bun run build

# 预览构建结果
bun run preview
```

### 桌面应用前端（配合 Tauri）

如果你要开发桌面应用，需要在 `tauri` 目录启动：

```bash
# 需要先确保后端已在 17493 端口运行（uv 或 conda 方式均可）
cd tauri
bun run tauri dev
```

### 营销网站（Landing Page）开发

```bash
cd landing
bun run dev
```

### 文档网站开发

```bash
cd docs
bun run dev
```

### 前端代码质量检查

```bash
# 在项目根目录执行

# Lint 检查
bun run lint

# TypeScript 类型检查
bun run typecheck

# 代码格式化
bun run format

# 自动修复所有问题
bun run check:fix
```

### Bun 常用命令

```bash
# 安装依赖
bun install

# 添加依赖
bun add <package-name>

# 添加开发依赖
bun add -d <package-name>

# 移除依赖
bun remove <package-name>

# 运行 package.json 中的脚本
bun run <script-name>
```

---

## 🖥️ 桌面应用开发

桌面应用使用 Tauri（Rust + Web 技术）开发，需要额外安装 Rust 工具链。

### 安装 Rust

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.bashrc  # 或 source ~/.zshrc
```

### macOS 额外依赖

```bash
xcode-select --install
```

### 启动桌面应用开发模式

**uv 方式启动后端：**
```bash
# 终端1：启动后端
source .venv/bin/activate  # 或直接用 uv run uvicorn ...
uvicorn backend.main:app --reload --port 17493

# 终端2：启动 Tauri 桌面应用
bun run dev
```

**或 Conda 方式启动后端：**
```bash
# 终端1：启动后端
conda activate voicebox
uvicorn backend.main:app --reload --port 17493

# 终端2：启动 Tauri 桌面应用
bun run dev
```

或者使用 just 命令（如果已安装 just）：

```bash
# 需要先配置好 Python 环境（venv 方式，或自行调整 justfile）
just dev
```

### 构建桌面应用

```bash
bun run build
```

---

## 🌐 Web 版本开发

Web 版本可以直接在浏览器中运行，无需安装桌面应用。

**uv 方式：**
```bash
# 终端1：启动后端
source .venv/bin/activate
uvicorn backend.main:app --reload --port 17493
# 或不激活环境：
# uv run uvicorn backend.main:app --reload --port 17493

# 终端2：启动 Web 前端
cd web
bun run dev
```

**或 Conda 方式：**
```bash
# 终端1：启动后端
conda activate voicebox
uvicorn backend.main:app --reload --port 17493

# 终端2：启动 Web 前端
cd web
bun run dev
```

访问 http://localhost:5173 使用 Web 版本。

---

## 🔌 端口说明

| 服务 | 默认端口 | 说明 |
|------|----------|------|
| 后端 API | 17493 | FastAPI 服务，提供 REST API 和 MCP 服务 |
| Web 前端 | 5173 | Vite 开发服务器 |
| API 文档 | 17493/docs | Swagger UI 交互式文档 |

---

## ❓ 常见问题

### Q: 如何确认后端启动成功？

访问 http://127.0.0.1:17493/health，如果返回 JSON 状态信息则表示成功：

```bash
curl http://127.0.0.1:17493/health
```

### Q: 模型下载慢怎么办？

可以设置 HuggingFace 镜像源：

```bash
# Linux/macOS
export HF_ENDPOINT=https://hf-mirror.com

# Windows PowerShell
# $env:HF_ENDPOINT = "https://hf-mirror.com"
```

### Q: 首次启动需要下载模型吗？

是的，首次使用某个 TTS/STT 引擎时会自动从 HuggingFace 下载模型。下载的模型会缓存到本地，后续启动无需重新下载。

### Q: 如何切换 GPU/CPU 后端？

后端会自动检测硬件环境：
- macOS Apple Silicon → 自动使用 MLX (Metal)
- NVIDIA GPU → 使用 CUDA 加速
- AMD GPU → 使用 ROCm 加速
- 其他 → CPU 回退

可以通过环境变量强制指定：

```bash
# 强制使用 CPU
VOICEBOX_FORCE_CPU=1 uvicorn backend.main:app --reload --port 17493
# 或 uv 方式：
# VOICEBOX_FORCE_CPU=1 uv run uvicorn backend.main:app --reload --port 17493
```

### Q: Conda、uv、venv 我该选哪个？

| 场景 | 推荐工具 |
|------|---------|
| 🚀 新项目、追求速度和现代体验 | **uv（推荐）** |
| 🧬 数据科学、需要管理 CUDA/CuDNN 等非 Python 依赖 | Conda |
| 📝 简单脚本、轻量需求 | venv + pip |
| 🎯 本项目开发 | **uv**（开箱即用，速度快，依赖解析准） |

对比表：

| 特性 | uv | Conda | venv |
|------|----|-------|------|
| Python 版本管理 | ✅ 自动下载管理 | ✅ | ❌ |
| 非 Python 依赖 | ⚠️ 有限（主要通过 pip） | ✅ CUDA/CuDNN 等 | ❌ |
| 速度 | 🚀 极快 | 🐢 慢 | 🐇 中等 |
| 依赖解析 | ✅ 可靠 | ⚠️ 容易冲突 | ⚠️ 容易卡住 |
| 锁文件 | ✅ uv.lock | ✅ environment.yml | ❌ requirements.txt |
| 项目隔离 | ✅ 自动 .venv | ❌ 全局环境 | ✅ |
| 跨平台 | ✅ | ✅ | ✅ |

### Q: uv 创建的虚拟环境在哪里？

uv 默认在项目根目录创建 `.venv` 文件夹，这是标准做法，IDE（如 VS Code、PyCharm）会自动识别。

### Q: 如何完全重置开发环境？

**uv 方式：**
```bash
# 1. 删除虚拟环境
deactivate 2>/dev/null; rm -rf .venv

# 2. 删除 node_modules
rm -rf node_modules app/node_modules tauri/node_modules web/node_modules landing/node_modules docs/node_modules

# 3. 清除 Python 缓存
find backend -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
find backend -type d -name "*.egg-info" -exec rm -rf {} + 2>/dev/null
find . -name "*.pyc" -delete 2>/dev/null

# 4. 重新创建环境并安装
uv venv --python 3.12
# 按照快速开始步骤重新安装依赖
```

**Conda 方式：**
```bash
# 1. 删除 Conda 环境
conda deactivate
conda env remove -n voicebox

# 2. 删除 node_modules
rm -rf node_modules app/node_modules tauri/node_modules web/node_modules landing/node_modules docs/node_modules

# 3. 清除 Python 缓存
find backend -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
find backend -type d -name "*.egg-info" -exec rm -rf {} + 2>/dev/null

# 4. 重新按照本文档步骤设置
```

### Q: uv 环境里能用 pip 吗？

可以，但推荐使用 `uv pip` 代替，因为它更快：

```bash
# 在激活的 uv 虚拟环境中：
uv pip install <package>   # 推荐
# 或 pip install <package>  # 也能用，但慢一些
```

---

## 📚 更多文档

- [官方文档](https://docs.voicebox.sh)（英文）
- [uv 官方文档](https://docs.astral.sh/uv/)
- [后端 README](backend/README.md)
- [贡献指南](CONTRIBUTING.md)
- [故障排除](docs/content/docs/overview/troubleshooting.mdx)（英文）

---

## 📝 开发命令速查表

### uv 方式（推荐）

| 操作 | 命令 |
|------|------|
| 创建虚拟环境 | `uv venv --python 3.12` |
| 激活环境（Linux/macOS） | `source .venv/bin/activate` |
| 激活环境（Windows） | `.venv\Scripts\activate` |
| 安装依赖 | `uv pip install -r backend/requirements.txt` |
| 启动后端（激活后） | `uvicorn backend.main:app --reload --port 17493` |
| 启动后端（无需激活） | `uv run uvicorn backend.main:app --reload --port 17493` |
| 后端 lint | `uv run ruff check backend/` |
| 后端测试 | `uv run pytest backend/tests -v` |

### Conda 方式

| 操作 | 命令 |
|------|------|
| 创建/激活环境 | `conda create -n voicebox python=3.12 -y && conda activate voicebox` |
| 启动后端 | `uvicorn backend.main:app --reload --port 17493` |
| 后端 lint | `ruff check backend/` |
| 后端测试 | `python -m pytest backend/tests -v` |

### 前端（通用）

| 操作 | 命令 |
|------|------|
| 安装前端依赖 | `bun install` |
| 启动 Web 前端 | `cd web && bun run dev` |
| 启动桌面应用 | `bun run dev`（需先启动后端） |
| 前端 lint | `bun run lint` |
| 类型检查 | `bun run typecheck` |
| 构建 Web 版本 | `cd web && bun run build` |

---

<p align="center">
  <a href="https://voicebox.sh">voicebox.sh</a>
</p>
