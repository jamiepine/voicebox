# Voicebox environment variables
# Copy to .env and override as needed

# Resource limits (docker-compose.yml)
CPU_LIMIT=4
MEMORY_LIMIT=8G

# ROCm overlay (docker-compose.rocm.yml)
# ROCM_VERSION=7.2
# HSA_OVERRIDE_GFX_VERSION=10.3.0
